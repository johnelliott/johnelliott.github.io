{
  "name": "blog",
  "version": "1.0.0",
  "description": "blog",
  "main": "build.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "node_modules/budo/bin/cmd.js --live --open",
    "sass-watch": "node_modules/node-sass/bin/node-sass --watch style.scss style.css",
    "sasquatch": "npm run sass-watch",
    "build": "node build.js && npm run build-css",
    "build-production": "npm run build && npm run minify-css",
    "build-css": "node_modules/node-sass/bin/node-sass style.scss style.css",
    "minify-css": "node_modules/clean-css/bin/cleancss style.css -o style.css --skip-restructuring"
  },
  "keywords": [
    "blog"
  ],
  "author": "John Elliott <johnelliott703@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "clean-css": "^3.4.19",
    "marked": "^0.3.5",
    "node-sass": "^3.8.0"
  },
  "dependencies": {
    "budo": "^9.2.1"
  }
}
